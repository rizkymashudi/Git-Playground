import type { Category } from '@/types/scenario';
import type { Repository } from '@/lib/git-engine/types';

export const CATEGORIES: Category[] = [
  {
    id: "basics",
    icon: "◆",
    title: "Fundamentals",
    color: "#4ade80",
    scenarios: [
      {
        id: "first-commit",
        title: "Your First Commit",
        difficulty: "Beginner",
        industry: "Any",
        description: "Learn the basic Git workflow: create files, stage them, and commit.",
        steps: [
          { instruction: "Create a new file called 'readme.md'", hint: "touch readme.md", check: (r: Repository) => r.workingDir.includes("readme.md") || r.staged.includes("readme.md") },
          { instruction: "Stage the file for commit", hint: "git add readme.md", check: (r: Repository) => r.staged.includes("readme.md") },
          { instruction: "Commit with message 'Add readme'", hint: "git commit -m 'Add readme'", check: (r: Repository) => r.commits.some(c => c.msg.toLowerCase().includes("readme")) },
          { instruction: "Check the log to see your commit", hint: "git log --oneline", check: () => true },
        ],
      },
      {
        id: "branching-basics",
        title: "Branch & Switch",
        difficulty: "Beginner",
        industry: "Any",
        description: "Create branches and switch between them — the foundation of parallel development.",
        steps: [
          { instruction: "Create a new branch called 'feature'", hint: "git branch feature", check: (r: Repository) => r.branches["feature"] },
          { instruction: "Switch to the 'feature' branch", hint: "git checkout feature", check: (r: Repository) => r.HEAD === "feature" },
          { instruction: "Create and stage a file 'feature.js'", hint: "touch feature.js && git add feature.js", check: (r: Repository) => r.staged.includes("feature.js") },
          { instruction: "Commit your feature work", hint: "git commit -m 'Add feature'", check: (r: Repository) => r.commits.some(c => c.branch === "feature" && c.msg !== "Initial commit") },
          { instruction: "Switch back to main", hint: "git checkout main", check: (r: Repository) => r.HEAD === "main" },
        ],
      },
      {
        id: "status-diff",
        title: "Understanding Status",
        difficulty: "Beginner",
        industry: "Any",
        description: "Learn to read git status and understand the staging area vs working directory.",
        steps: [
          { instruction: "Check the current status", hint: "git status", check: () => true },
          { instruction: "Create two files: 'app.js' and 'style.css'", hint: "touch app.js\nthen: touch style.css", check: (r: Repository) => (r.workingDir.includes("app.js") || r.staged.includes("app.js")) && (r.workingDir.includes("style.css") || r.staged.includes("style.css")) },
          { instruction: "Stage only 'app.js' (not style.css)", hint: "git add app.js", check: (r: Repository) => r.staged.includes("app.js") && !r.staged.includes("style.css") },
          { instruction: "Check status again — notice the difference!", hint: "git status", check: () => true },
          { instruction: "Now add everything and commit", hint: "git add . && git commit -m 'Add files'", check: (r: Repository) => r.staged.length === 0 && r.commits.length > 1 },
        ],
      },
    ],
  },
  {
    id: "collaboration",
    icon: "◈",
    title: "Collaboration",
    color: "#818cf8",
    scenarios: [
      {
        id: "feature-branch",
        title: "Feature Branch Workflow",
        difficulty: "Intermediate",
        industry: "Tech Startups / SaaS",
        description: "The most common workflow in industry. Create a feature branch, develop, then merge back.",
        steps: [
          { instruction: "Create and switch to 'feature/login-page'", hint: "git checkout -b feature/login-page", check: (r: Repository) => r.HEAD === "feature/login-page" },
          { instruction: "Add login.jsx and commit", hint: "touch login.jsx && git add . && git commit -m 'Add login page'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/login-page") },
          { instruction: "Switch back to main", hint: "git checkout main", check: (r: Repository) => r.HEAD === "main" },
          { instruction: "Merge the feature branch into main", hint: "git merge feature/login-page", check: (r: Repository) => r.commits.some(c => c.msg.includes("Merge")) },
          { instruction: "Push to remote", hint: "git push origin main", check: (r: Repository) => r.remotes.origin?.main },
        ],
      },
      {
        id: "code-review-flow",
        title: "Pull Request Simulation",
        difficulty: "Intermediate",
        industry: "Enterprise / FAANG",
        description: "Simulate the PR workflow: branch → commit → push → review cycle.",
        steps: [
          { instruction: "Create branch 'fix/auth-bug-2341'", hint: "git checkout -b fix/auth-bug-2341", check: (r: Repository) => r.HEAD === "fix/auth-bug-2341" },
          { instruction: "Create fix file and commit", hint: "touch auth-fix.swift && git add . && git commit -m 'Fix auth token refresh'", check: (r: Repository) => r.commits.some(c => c.branch === "fix/auth-bug-2341") },
          { instruction: "Push the branch to origin for PR", hint: "git push origin fix/auth-bug-2341", check: (r: Repository) => r.remotes.origin?.["fix/auth-bug-2341"] },
          { instruction: "Switch to main and merge (simulating PR approval)", hint: "git checkout main && git merge fix/auth-bug-2341", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge")) },
          { instruction: "Clean up: delete the feature branch", hint: "git branch -d fix/auth-bug-2341", check: (r: Repository) => !r.branches["fix/auth-bug-2341"] },
        ],
      },
    ],
  },
  {
    id: "advanced",
    icon: "◇",
    title: "Advanced Ops",
    color: "#f472b6",
    scenarios: [
      {
        id: "rebase-workflow",
        title: "Rebase vs Merge",
        difficulty: "Advanced",
        industry: "Open Source / Linux Kernel",
        description: "Learn rebase for a clean linear history — preferred in many large-scale projects.",
        steps: [
          { instruction: "Create and switch to 'feature/parser'", hint: "git checkout -b feature/parser", check: (r: Repository) => r.HEAD === "feature/parser" },
          { instruction: "Make a commit on feature branch", hint: "touch parser.rs && git add . && git commit -m 'Add parser module'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/parser") },
          { instruction: "Switch to main and make a commit there too", hint: "git checkout main && touch config.rs && git add . && git commit -m 'Add config'", check: (r: Repository) => r.HEAD === "main" && r.commits.filter(c => c.branch === "main").length > 1 },
          { instruction: "Switch to feature and rebase onto main", hint: "git checkout feature/parser && git rebase main", check: (r: Repository) => r.HEAD === "feature/parser" && r.commits.some(c => c.msg.includes("Rebased")) },
          { instruction: "Switch to main and merge (fast-forward)", hint: "git checkout main && git merge feature/parser", check: (r: Repository) => r.HEAD === "main" },
        ],
      },
      {
        id: "stash-workflow",
        title: "Stash & Context Switch",
        difficulty: "Intermediate",
        industry: "Agency / Multiple Clients",
        description: "Stash lets you save WIP changes when you need to switch context urgently.",
        steps: [
          { instruction: "Start working: create 'wip-feature.ts'", hint: "touch wip-feature.ts", check: (r: Repository) => r.workingDir.includes("wip-feature.ts") || r.staged.includes("wip-feature.ts") },
          { instruction: "Stage your work in progress", hint: "git add .", check: (r: Repository) => r.staged.includes("wip-feature.ts") },
          { instruction: "Urgent bug! Stash your changes", hint: "git stash", check: (r: Repository) => r.stash.length > 0 && r.staged.length === 0 },
          { instruction: "Fix the bug: create 'hotfix.ts', commit it", hint: "touch hotfix.ts && git add . && git commit -m 'Hotfix: critical bug'", check: (r: Repository) => r.commits.some(c => c.msg.toLowerCase().includes("hotfix") || c.msg.toLowerCase().includes("bug")) },
          { instruction: "Restore your WIP with stash pop", hint: "git stash pop", check: (r: Repository) => r.stash.length === 0 && r.staged.length > 0 },
        ],
      },
      {
        id: "cherry-pick-flow",
        title: "Cherry-Pick Hotfix",
        difficulty: "Advanced",
        industry: "Mobile Apps / Release Mgmt",
        description: "Apply a specific commit from one branch to another — essential for hotfix workflows.",
        steps: [
          { instruction: "Create 'develop' branch with a new commit", hint: "git checkout -b develop && touch api.kt && git add . && git commit -m 'New API endpoint'", check: (r: Repository) => r.branches.develop },
          { instruction: "Add another commit (the hotfix we want)", hint: "touch hotfix.kt && git add . && git commit -m 'Critical security patch'", check: (r: Repository) => r.commits.some(c => c.msg.toLowerCase().includes("security") || c.msg.toLowerCase().includes("patch")) },
          { instruction: "Switch to main (production)", hint: "git checkout main", check: (r: Repository) => r.HEAD === "main" },
          { instruction: "Cherry-pick ONLY the security patch commit (use its hash from 'git log --oneline' on develop)", hint: "git log --oneline (to find hash), then git cherry-pick <hash>", check: (r: Repository) => r.commits.some(c => c.cherryPicked) },
        ],
      },
      {
        id: "release-tagging",
        title: "Release Tagging",
        difficulty: "Intermediate",
        industry: "DevOps / CI/CD",
        description: "Tag releases for deployment — maps to production releases and rollback strategies.",
        steps: [
          { instruction: "Make a commit representing v1.0 work", hint: "touch app.py && git add . && git commit -m 'Release v1.0 features'", check: (r: Repository) => r.commits.length > 1 },
          { instruction: "Tag this commit as v1.0.0", hint: "git tag v1.0.0", check: (r: Repository) => r.tags["v1.0.0"] },
          { instruction: "Add more work and commit for v1.1", hint: "touch newfeature.py && git add . && git commit -m 'Add new feature for v1.1'", check: (r: Repository) => r.commits.length > 2 },
          { instruction: "Tag as v1.1.0", hint: "git tag v1.1.0", check: (r: Repository) => r.tags["v1.1.0"] },
          { instruction: "List all tags to verify", hint: "git tag", check: () => true },
        ],
      },
    ],
  },
  {
    id: "rescue",
    icon: "⬡",
    title: "Rescue & Recovery",
    color: "#fb923c",
    scenarios: [
      {
        id: "reset-modes",
        title: "Undo with Reset",
        difficulty: "Advanced",
        industry: "Any — Disaster Recovery",
        description: "Master git reset --soft, --mixed, and --hard to undo mistakes safely.",
        steps: [
          { instruction: "Create files, stage, and commit", hint: "touch a.txt b.txt && git add . && git commit -m 'Add files'", check: (r: Repository) => r.commits.length > 1 },
          { instruction: "Create more changes and stage them", hint: "touch c.txt && git add c.txt", check: (r: Repository) => r.staged.length > 0 },
          { instruction: "Use git reset (mixed) to unstage", hint: "git reset", check: (r: Repository) => r.staged.length === 0 },
          { instruction: "Stage and commit again", hint: "git add . && git commit -m 'Second commit'", check: (r: Repository) => r.commits.length > 2 },
          { instruction: "Use reset --hard to discard everything", hint: "touch temp.txt && git add . && git reset --hard", check: (r: Repository) => r.staged.length === 0 && r.workingDir.length === 0 },
        ],
      },
      {
        id: "reflog-rescue",
        title: "Reflog Time Travel",
        difficulty: "Expert",
        industry: "Any — 'I broke everything'",
        description: "Use reflog to find and recover lost commits — the ultimate safety net.",
        steps: [
          { instruction: "Make two commits to build history", hint: "touch a.txt && git add . && git commit -m 'First'\nthen: touch b.txt && git add . && git commit -m 'Second'", check: (r: Repository) => r.commits.length >= 3 },
          { instruction: "Use reflog to see all HEAD movements", hint: "git reflog", check: () => true },
          { instruction: "Hard reset to discard last commit", hint: "git reset --hard", check: () => true },
          { instruction: "Check reflog again — the 'lost' commit is there!", hint: "git reflog", check: () => true },
        ],
      },
    ],
  },
  {
    id: "workflows",
    icon: "⬢",
    title: "Industry Workflows",
    color: "#22d3ee",
    scenarios: [
      {
        id: "gitflow-full",
        title: "GitFlow Workflow",
        difficulty: "Advanced",
        industry: "Enterprise / Regulated",
        description: "The classic structured workflow: main, develop, feature/*, release/*, and hotfix/* branches.",
        steps: [
          { instruction: "Create the 'develop' integration branch", hint: "git branch develop", check: (r: Repository) => r.branches.develop },
          { instruction: "Switch to develop and create feature branch", hint: "git checkout develop && git checkout -b feature/user-auth", check: (r: Repository) => r.HEAD === "feature/user-auth" },
          { instruction: "Build the feature: add auth.swift and commit", hint: "touch auth.swift && git add . && git commit -m 'Implement user auth'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/user-auth") },
          { instruction: "Merge feature back to develop", hint: "git checkout develop && git merge feature/user-auth", check: (r: Repository) => r.HEAD === "develop" && r.commits.some(c => c.msg.includes("Merge") && c.branch === "develop") },
          { instruction: "Create release branch from develop", hint: "git checkout -b release/1.0", check: (r: Repository) => r.HEAD === "release/1.0" },
          { instruction: "Finalize release: add changelog and commit", hint: "touch CHANGELOG.md && git add . && git commit -m 'Prepare release 1.0'", check: (r: Repository) => r.commits.some(c => c.branch === "release/1.0") },
          { instruction: "Merge release into main and tag it", hint: "git checkout main && git merge release/1.0 && git tag v1.0", check: (r: Repository) => r.HEAD === "main" && r.tags["v1.0"] },
        ],
      },
      {
        id: "github-flow",
        title: "GitHub Flow",
        difficulty: "Beginner",
        industry: "SaaS / Web Apps",
        description: "Lightweight workflow: branch from main, commit, open PR, merge, deploy. Used by most startups.",
        steps: [
          { instruction: "Create feature branch from main", hint: "git checkout -b feature/dark-mode", check: (r: Repository) => r.HEAD === "feature/dark-mode" },
          { instruction: "Add your changes and commit", hint: "touch dark-mode.tsx && git add . && git commit -m 'Add dark mode toggle'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/dark-mode") },
          { instruction: "Push branch to remote (opens PR)", hint: "git push origin feature/dark-mode", check: (r: Repository) => r.remotes.origin?.["feature/dark-mode"] },
          { instruction: "Switch to main and merge (PR approved!)", hint: "git checkout main && git merge feature/dark-mode", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge")) },
          { instruction: "Deploy: push main to origin", hint: "git push origin main", check: (r: Repository) => r.remotes.origin?.main },
          { instruction: "Clean up: delete feature branch", hint: "git branch -d feature/dark-mode", check: (r: Repository) => !r.branches["feature/dark-mode"] },
        ],
      },
      {
        id: "gitlab-flow",
        title: "GitLab Flow",
        difficulty: "Intermediate",
        industry: "DevOps / Staging Envs",
        description: "GitHub Flow + environment branches. Code flows: feature → main → staging → production.",
        steps: [
          { instruction: "Create environment branches", hint: "git branch staging && git branch production", check: (r: Repository) => r.branches.staging && r.branches.production },
          { instruction: "Create a feature branch and work on it", hint: "git checkout -b feature/notifications && touch notif.py && git add . && git commit -m 'Add push notifications'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/notifications") },
          { instruction: "Merge feature into main", hint: "git checkout main && git merge feature/notifications", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge")) },
          { instruction: "Promote main to staging for QA", hint: "git checkout staging && git merge main", check: (r: Repository) => r.HEAD === "staging" },
          { instruction: "After QA passes, promote to production", hint: "git checkout production && git merge staging && git tag v2.1", check: (r: Repository) => r.HEAD === "production" && r.tags["v2.1"] },
        ],
      },
      {
        id: "forking-flow",
        title: "Forking Workflow",
        difficulty: "Intermediate",
        industry: "Open Source / OSS",
        description: "Each contributor works on their own fork. Used in Linux, React, Swift, and most GitHub OSS projects.",
        steps: [
          { instruction: "Simulate fork: add 'upstream' remote", hint: "git remote add upstream", check: (r: Repository) => r.remotes.upstream },
          { instruction: "Create your feature branch", hint: "git checkout -b fix/memory-leak", check: (r: Repository) => r.HEAD === "fix/memory-leak" },
          { instruction: "Make your contribution and commit", hint: "touch leak-fix.c && git add . && git commit -m 'Fix memory leak in parser'", check: (r: Repository) => r.commits.some(c => c.branch === "fix/memory-leak") },
          { instruction: "Push to YOUR origin (your fork)", hint: "git push origin fix/memory-leak", check: (r: Repository) => r.remotes.origin?.["fix/memory-leak"] },
          { instruction: "Simulate upstream merge: switch to main and merge", hint: "git checkout main && git merge fix/memory-leak", check: (r: Repository) => r.HEAD === "main" },
          { instruction: "Sync with upstream: fetch and pull", hint: "git fetch", check: () => true },
        ],
      },
      {
        id: "release-branch-flow",
        title: "Release Branching",
        difficulty: "Advanced",
        industry: "Mobile Apps / iOS & Android",
        description: "Maintain parallel release branches. Essential for App Store review cycles and multi-version support.",
        steps: [
          { instruction: "Develop features on main", hint: "touch feature-a.swift && git add . && git commit -m 'Feature A for v2.0'", check: (r: Repository) => r.commits.length > 1 },
          { instruction: "Cut release branch for v2.0", hint: "git checkout -b release/2.0", check: (r: Repository) => r.HEAD === "release/2.0" },
          { instruction: "Bugfix on release branch", hint: "touch bugfix.swift && git add . && git commit -m 'Fix crash on launch'", check: (r: Repository) => r.commits.some(c => c.branch === "release/2.0" && c.msg !== "Initial commit") },
          { instruction: "Tag the release candidate", hint: "git tag v2.0.0-rc1", check: (r: Repository) => r.tags["v2.0.0-rc1"] },
          { instruction: "Meanwhile, continue dev on main for v2.1", hint: "git checkout main && touch feature-b.swift && git add . && git commit -m 'Feature B for v2.1'", check: (r: Repository) => r.HEAD === "main" && r.commits.filter(c => c.branch === "main").length > 2 },
          { instruction: "Hotfix on release: cherry-pick or commit", hint: "git checkout release/2.0 && touch hotfix.swift && git add . && git commit -m 'Hotfix: critical v2.0 bug' && git tag v2.0.0", check: (r: Repository) => r.tags["v2.0.0"] },
        ],
      },
      {
        id: "env-branch-flow",
        title: "Environment Branching",
        difficulty: "Intermediate",
        industry: "Enterprise / Multi-Env",
        description: "Branches mirror deployment environments: dev → staging → production. Popular in enterprise CI/CD.",
        steps: [
          { instruction: "Create environment branches: dev, staging, production", hint: "git branch dev && git branch staging && git branch production", check: (r: Repository) => r.branches.dev && r.branches.staging && r.branches.production },
          { instruction: "Work on dev branch", hint: "git checkout dev && touch api-v2.go && git add . && git commit -m 'New API v2 endpoints'", check: (r: Repository) => r.commits.some(c => c.branch === "dev") },
          { instruction: "Promote to staging after dev review", hint: "git checkout staging && git merge dev", check: (r: Repository) => r.HEAD === "staging" },
          { instruction: "Run QA on staging, then promote to production", hint: "git checkout production && git merge staging", check: (r: Repository) => r.HEAD === "production" },
          { instruction: "Tag the production release", hint: "git tag v3.0-prod", check: (r: Repository) => r.tags["v3.0-prod"] },
          { instruction: "Continue work: switch back to dev", hint: "git checkout dev", check: (r: Repository) => r.HEAD === "dev" },
        ],
      },
      {
        id: "trunk-based-flow",
        title: "Trunk-Based Development",
        difficulty: "Advanced",
        industry: "Google / Netflix / CI/CD",
        description: "Everyone commits to main. Short-lived branches (<1 day). Requires feature flags and strong CI.",
        steps: [
          { instruction: "Create a very short-lived branch", hint: "git checkout -b quick-fix", check: (r: Repository) => r.HEAD === "quick-fix" },
          { instruction: "Make a small, focused change", hint: "touch fix.ts && git add . && git commit -m 'Fix button alignment'", check: (r: Repository) => r.commits.some(c => c.branch === "quick-fix") },
          { instruction: "Immediately merge back to main", hint: "git checkout main && git merge quick-fix", check: (r: Repository) => r.HEAD === "main" },
          { instruction: "Delete the branch (keep it short-lived!)", hint: "git branch -d quick-fix", check: (r: Repository) => !r.branches["quick-fix"] },
          { instruction: "Another quick iteration: branch, commit, merge", hint: "git checkout -b small-tweak && touch tweak.ts && git add . && git commit -m 'Tweak nav spacing' && git checkout main && git merge small-tweak", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Tweak")) },
          { instruction: "Push to deploy (trunk is always deployable)", hint: "git push origin main", check: (r: Repository) => r.remotes.origin?.main },
        ],
      },
      {
        id: "ship-show-ask-flow",
        title: "Ship / Show / Ask",
        difficulty: "Intermediate",
        industry: "High-Trust Teams / Basecamp",
        description: "Three merge paths based on confidence: Ship (merge directly), Show (merge then review), Ask (PR first).",
        steps: [
          { instruction: "SHIP path — confident fix, merge directly to main", hint: "git checkout -b ship/typo-fix && touch typo-fix.md && git add . && git commit -m 'Fix typo in docs'", check: (r: Repository) => r.commits.some(c => c.branch === "ship/typo-fix") },
          { instruction: "Merge directly (no review needed)", hint: "git checkout main && git merge ship/typo-fix && git branch -d ship/typo-fix", check: (r: Repository) => r.HEAD === "main" && !r.branches["ship/typo-fix"] },
          { instruction: "SHOW path — merge first, then notify team for async review", hint: "git checkout -b show/refactor-utils && touch utils.ts && git add . && git commit -m 'Refactor utility helpers'", check: (r: Repository) => r.commits.some(c => c.branch === "show/refactor-utils") },
          { instruction: "Merge to main, push (team reviews after)", hint: "git checkout main && git merge show/refactor-utils && git push origin main", check: (r: Repository) => r.HEAD === "main" && r.remotes.origin?.main },
          { instruction: "ASK path — needs discussion, push branch for PR", hint: "git checkout -b ask/new-auth-flow && touch auth-v2.ts && git add . && git commit -m 'Propose new auth flow'", check: (r: Repository) => r.commits.some(c => c.branch === "ask/new-auth-flow") },
          { instruction: "Push branch for review (don't merge yet!)", hint: "git push origin ask/new-auth-flow", check: (r: Repository) => r.remotes.origin?.["ask/new-auth-flow"] },
        ],
      },
      {
        id: "feature-flag-flow",
        title: "Feature Flag Flow",
        difficulty: "Advanced",
        industry: "SaaS / Product Teams",
        description: "Merge incomplete features behind flags. Deploy daily, enable gradually. Used by LaunchDarkly, Spotify.",
        steps: [
          { instruction: "Create branch for a new feature behind a flag", hint: "git checkout -b feature/new-checkout", check: (r: Repository) => r.HEAD === "feature/new-checkout" },
          { instruction: "Add the feature code (disabled by default)", hint: "touch checkout-v2.tsx && git add . && git commit -m 'Add checkout v2 behind feature flag'", check: (r: Repository) => r.commits.some(c => c.branch === "feature/new-checkout" && c.msg.toLowerCase().includes("flag")) },
          { instruction: "Merge to main immediately (flag keeps it hidden)", hint: "git checkout main && git merge feature/new-checkout", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge")) },
          { instruction: "Deploy main — feature is hidden in production", hint: "git push origin main && git tag deploy-2024-03-15", check: (r: Repository) => r.remotes.origin?.main && Object.keys(r.tags).length > 0 },
          { instruction: "Later: enable flag for 10% of users (commit config change)", hint: "touch flag-config.json && git add . && git commit -m 'Enable checkout-v2 for 10% rollout'", check: (r: Repository) => r.commits.some(c => c.msg.toLowerCase().includes("rollout") || c.msg.toLowerCase().includes("enable")) },
          { instruction: "Full rollout: remove the flag", hint: "touch cleanup.ts && git add . && git commit -m 'Remove feature flag, checkout v2 is GA'", check: (r: Repository) => r.commits.some(c => c.msg.toLowerCase().includes("remove") || c.msg.toLowerCase().includes("ga")) },
        ],
      },
      {
        id: "stacked-pr-flow",
        title: "Stacked PRs",
        difficulty: "Advanced",
        industry: "Meta / Graphite / Large Codebases",
        description: "Break large features into a chain of dependent branches. Review and merge incrementally.",
        steps: [
          { instruction: "Start the base layer: data model changes", hint: "git checkout -b stack/1-data-model", check: (r: Repository) => r.HEAD === "stack/1-data-model" },
          { instruction: "Build the data layer and commit", hint: "touch models.swift && git add . && git commit -m 'Add new data models for profiles'", check: (r: Repository) => r.commits.some(c => c.branch === "stack/1-data-model") },
          { instruction: "Stack layer 2 ON TOP of layer 1 (not from main!)", hint: "git checkout -b stack/2-api-layer", check: (r: Repository) => r.HEAD === "stack/2-api-layer" },
          { instruction: "Build the API layer", hint: "touch api-client.swift && git add . && git commit -m 'Add API client for profiles'", check: (r: Repository) => r.commits.some(c => c.branch === "stack/2-api-layer") },
          { instruction: "Stack layer 3: the UI on top of API", hint: "git checkout -b stack/3-ui-layer && touch profile-view.swift && git add . && git commit -m 'Add profile UI components'", check: (r: Repository) => r.commits.some(c => c.branch === "stack/3-ui-layer") },
          { instruction: "Merge the stack bottom-up: merge layer 1 into main first", hint: "git checkout main && git merge stack/1-data-model", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge") && c.msg.includes("stack/1")) },
          { instruction: "Then merge layer 2, then layer 3", hint: "git merge stack/2-api-layer && git merge stack/3-ui-layer", check: (r: Repository) => r.commits.filter(c => c.msg.includes("Merge")).length >= 3 },
        ],
      },
      {
        id: "hotfix-only-flow",
        title: "Hotfix-Only Pattern",
        difficulty: "Intermediate",
        industry: "Production Incidents / On-Call",
        description: "Emergency-only branching from production tags. Bypass normal flow for critical fixes.",
        steps: [
          { instruction: "You're on main with a production release tagged", hint: "touch app.py && git add . && git commit -m 'Release v3.0' && git tag v3.0", check: (r: Repository) => r.tags["v3.0"] },
          { instruction: "ALERT! Critical bug in production. Branch from the tag", hint: "git checkout -b hotfix/critical-crash", check: (r: Repository) => r.HEAD === "hotfix/critical-crash" },
          { instruction: "Apply the emergency fix", hint: "touch crash-fix.py && git add . && git commit -m 'HOTFIX: Fix null pointer crash on login'", check: (r: Repository) => r.commits.some(c => c.branch === "hotfix/critical-crash" && (c.msg.includes("HOTFIX") || c.msg.includes("hotfix"))) },
          { instruction: "Merge hotfix to main and tag the patch release", hint: "git checkout main && git merge hotfix/critical-crash && git tag v3.0.1", check: (r: Repository) => r.HEAD === "main" && r.tags["v3.0.1"] },
          { instruction: "Push the fix and deploy immediately", hint: "git push origin main", check: (r: Repository) => r.remotes.origin?.main },
          { instruction: "Clean up the hotfix branch", hint: "git branch -d hotfix/critical-crash", check: (r: Repository) => !r.branches["hotfix/critical-crash"] },
        ],
      },
      {
        id: "monorepo-flow",
        title: "Monorepo Workflow",
        difficulty: "Advanced",
        industry: "Google / Uber / Monorepos",
        description: "Single repo, multiple packages. Teams own paths, merge to main with path-based CI triggers.",
        steps: [
          { instruction: "Create a feature branch scoped to your team's package", hint: "git checkout -b team-ios/sdk-localization", check: (r: Repository) => r.HEAD === "team-ios/sdk-localization" },
          { instruction: "Work on your package only", hint: "touch packages/localization/cache.swift && git add . && git commit -m 'Add memory cache tier to localization SDK'", check: (r: Repository) => r.commits.some(c => c.branch === "team-ios/sdk-localization") },
          { instruction: "Another team works in parallel on their package", hint: "git checkout main && git checkout -b team-web/dashboard && touch packages/web/dashboard.tsx && git add . && git commit -m 'Revamp analytics dashboard'", check: (r: Repository) => r.branches["team-web/dashboard"] },
          { instruction: "Merge your SDK work to main first", hint: "git checkout main && git merge team-ios/sdk-localization", check: (r: Repository) => r.HEAD === "main" && r.commits.some(c => c.msg.includes("Merge") && c.msg.includes("team-ios")) },
          { instruction: "Then merge the web team's work (no conflicts, different paths)", hint: "git merge team-web/dashboard", check: (r: Repository) => r.commits.filter(c => c.msg.includes("Merge")).length >= 2 },
          { instruction: "Tag a unified release from main", hint: "git tag monorepo-2024.03 && git push origin main", check: (r: Repository) => Object.keys(r.tags).some(t => t.includes("monorepo")) },
        ],
      },
    ],
  },
];
